// import Controller — handles HTTP requests
// Pattern: req → controller → service → repository → response
// TODO: Implement each method

module.exports = {
    importProducts: async (req, res, next) => { res.json({ message: 'TODO' }); },
    importCategories: async (req, res, next) => { res.json({ message: 'TODO' }); }
};
