// notification Controller — handles HTTP requests
// Pattern: req → controller → service → repository → response
// TODO: Implement each method

module.exports = {
    getAll: async (req, res, next) => { res.json({ message: 'TODO' }); },
    getUnreadCount: async (req, res, next) => { res.json({ message: 'TODO' }); },
    markAsRead: async (req, res, next) => { res.json({ message: 'TODO' }); },
    markAllAsRead: async (req, res, next) => { res.json({ message: 'TODO' }); }
};
