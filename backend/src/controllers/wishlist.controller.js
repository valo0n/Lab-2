// wishlist Controller — handles HTTP requests
// Pattern: req → controller → service → repository → response
// TODO: Implement each method

module.exports = {
    getAll: async (req, res, next) => { res.json({ message: 'TODO' }); },
    add: async (req, res, next) => { res.json({ message: 'TODO' }); },
    remove: async (req, res, next) => { res.json({ message: 'TODO' }); }
};
