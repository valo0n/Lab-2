// export Controller — handles HTTP requests
// Pattern: req → controller → service → repository → response
// TODO: Implement each method

module.exports = {
    exportData: async (req, res, next) => { res.json({ message: 'TODO' }); }
};
