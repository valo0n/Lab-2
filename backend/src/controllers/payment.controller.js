// payment Controller — handles HTTP requests
// Pattern: req → controller → service → repository → response
// TODO: Implement each method

module.exports = {
    createIntent: async (req, res, next) => { res.json({ message: 'TODO' }); },
    webhook: async (req, res, next) => { res.json({ message: 'TODO' }); },
    getByOrder: async (req, res, next) => { res.json({ message: 'TODO' }); }
};
