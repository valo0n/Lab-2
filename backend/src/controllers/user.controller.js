// user Controller — handles HTTP requests
// Pattern: req → controller → service → repository → response
// TODO: Implement each method

module.exports = {
    getProfile: async (req, res, next) => { res.json({ message: 'TODO' }); },
    updateProfile: async (req, res, next) => { res.json({ message: 'TODO' }); },
    getAddresses: async (req, res, next) => { res.json({ message: 'TODO' }); },
    addAddress: async (req, res, next) => { res.json({ message: 'TODO' }); },
    updateAddress: async (req, res, next) => { res.json({ message: 'TODO' }); },
    deleteAddress: async (req, res, next) => { res.json({ message: 'TODO' }); }
};
