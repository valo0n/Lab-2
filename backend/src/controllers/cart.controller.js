// cart Controller — handles HTTP requests
// Pattern: req → controller → service → repository → response
// TODO: Implement each method

module.exports = {
    getCart: async (req, res, next) => { res.json({ message: 'TODO' }); },
    addItem: async (req, res, next) => { res.json({ message: 'TODO' }); },
    updateItem: async (req, res, next) => { res.json({ message: 'TODO' }); },
    removeItem: async (req, res, next) => { res.json({ message: 'TODO' }); },
    clearCart: async (req, res, next) => { res.json({ message: 'TODO' }); }
};
