// newsletter Controller — handles HTTP requests
// Pattern: req → controller → service → repository → response
// TODO: Implement each method

module.exports = {
    subscribe: async (req, res, next) => { res.json({ message: 'TODO' }); },
    unsubscribe: async (req, res, next) => { res.json({ message: 'TODO' }); }
};
