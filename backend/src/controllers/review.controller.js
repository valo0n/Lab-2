// review Controller — handles HTTP requests
// Pattern: req → controller → service → repository → response
// TODO: Implement each method

module.exports = {
    getByProduct: async (req, res, next) => { res.json({ message: 'TODO' }); },
    create: async (req, res, next) => { res.json({ message: 'TODO' }); },
    approve: async (req, res, next) => { res.json({ message: 'TODO' }); },
    delete: async (req, res, next) => { res.json({ message: 'TODO' }); }
};
